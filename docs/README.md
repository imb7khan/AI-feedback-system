# Project documentation (markdown)

Design notes, migration summaries, and implementation write-ups live here.

**Handbooks at repo root:** `CLAUDE.md`, `BUILD_LOG.md`

**This folder:**

- **Root of `docs/`** — project-wide summaries (RAG, pipeline, LangChain, plagiarism, JSON, etc.)
- **`feedback_system/`** — vector RAG docs
- **`feedback-frontend/`** — frontend setup and CRA README

Internal links inside older files may still mention old paths (e.g. `feedback_system/VECTOR_RAG_README.md`); files are now under `docs/…`.

**Tests** were moved to repo root `tests/` (`integration/`, `feedback_system/`, `fixtures/`). See `tests/README.md` for commands.
