# OBJECTIVE (FIRST PROMPT)

You are helping me build an AI-powered Student Assignment Feedback Generator.

The system should:

* Accept student essays (PDF/DOCX/Text)
* Extract and process text
* Split content into paragraphs
* Provide paragraph-level feedback
* Evaluate using a structured rubric
* Use a simple RAG system to retrieve rubric
* Use a single agent to orchestrate evaluation
* Return structured JSON output with scores and feedback

Constraints:

* Keep architecture simple and modular
* Do NOT use multi-agent systems
* Use Django for backend
* Use LLM (Claude/OpenAI) for feedback
* Use simple RAG (no overengineering)

We will build this step-by-step. Do NOT implement everything at once. Wait for my next instruction after each step.

Create a Django backend project for this system.

Requirements:

* Create a Django project and app (e.g., "feedback_system")
* Setup basic REST API structure
* Add endpoint for file upload
* Keep code clean and modular

Do NOT implement AI logic yet.
Only setup project structure and basic API endpoints.

Explain folder structure clearly.

Now implement file upload and text extraction.

Requirements:

Accept PDF, DOCX, and TXT files
Extract text using Python libraries
Return extracted text via API

Make sure:

Code is clean
Error handling is included
Output is plain text

Do NOT implement AI yet.

Now implement text preprocessing.

Requirements:

Split extracted text into paragraphs
Clean unnecessary whitespace
Return list of paragraphs

Keep logic simple and reliable.

Do NOT add AI yet.

Now integrate LLM (Claude or OpenAI) Using Groq .

Requirements:

* Create a function to send prompt to LLM
* Generate paragraph-level feedback
* Use structured prompt
* Return feedback for each paragraph

Important:

* Keep temperature low (0.2)
* Ensure consistent output

Do NOT implement RAG or agent yet.

Now define a rubric system.

Requirements:

* Create a structured rubric (argument, evidence, grammar)
* Store it in JSON or database
* Make it retrievable

Do NOT implement RAG yet.

Now implement simple RAG.

Requirements:

* Retrieve rubric from storage
* Inject rubric into LLM prompt
* Use it for scoring

Keep it simple:

* No complex vector DB required yet

Explain clearly how RAG is being used.

Now implement a single agent to orchestrate the workflow.

Agent responsibilities:

* Take essay paragraphs
* Call LLM for feedback
* Retrieve rubric (RAG)
* Generate scores
* Combine everything into final output

Keep design simple:

* One agent only
* No multi-agent system

Explain architecture clearly.

Now enforce structured JSON output.

Requirements:

* Paragraph feedback
* Rubric scores
* Overall feedback
* Suggestions

Ensure:

* Strict JSON format
* No random text outside JSON

Provide example response.

Now connect everything into a full pipeline.

Flow:

Upload → Extract → Split → Agent → Output

Ensure:

End-to-end working API
Clean integration

Test with sample input.

Now create a simple React frontend.

Requirements:

Upload essay
Display feedback
Show scores clearly

Keep UI minimal using Tailwind CSS.  

Now improve system quality.

Tasks:

Add error handling
Improve prompts
Ensure consistent grading
Test edge cases

Do NOT add unnecessary complexity.

You are a senior Python/Django + LLM engineer. I have an existing Django project where current “RAG” is rubric retrieval from relational DB (SQLite) and prompt injection only (no embeddings/vector DB). I want you to upgrade it to a true vector-embedding RAG pipeline.

Goal:
Implement production-ready embedding-based retrieval for rubric-aware feedback generation.

Current context:
- Framework: Django
- Existing app: feedback_system
- Existing files include:
  - feedback_system/rubric_rag.py
  - feedback_system/llm_integration.py
  - feedback_system/feedback_agent.py
  - assignment_feedback/settings.py
- Current retrieval uses Django ORM filter queries, not semantic search.

What I need from you:
1) Architecture decision
- Propose best vector stack for this project (Chroma or FAISS for local dev, and optional Pinecone/Qdrant for scalable deployment).
- Explain tradeoffs briefly, then choose one default implementation.
- Keep existing non-vector fallback mode.

2) Embedding integration
- Add embedding model integration (recommend one strong default, configurable by env vars).
- Create chunking strategy for rubric content:
  - rubric
  - category
  - criterion
  - performance levels
- Store metadata with each vector:
  - rubric_id, assignment_type, category, criterion, level, source_text, timestamps.

3) Indexing pipeline
- Implement command/service to build and refresh vector index from DB rubrics.
- Handle re-index on rubric create/update/delete.
- Add idempotent upsert behavior.
- Include logging and error handling.

4) Retrieval pipeline (true RAG)
- Query embeddings from user input (assignment text / paragraph).
- Retrieve top-k relevant chunks semantically.
- Add optional metadata filtering (assignment_type, rubric_id).
- Add reranking or scoring normalization if useful.
- Inject retrieved context into LLM prompt in structured format.

5) Code changes
- Modify existing services minimally but cleanly.
- Keep class/function names readable and backward compatible where possible.
- Add config in settings/env:
  - VECTOR_DB_PROVIDER
  - EMBEDDING_MODEL
  - VECTOR_TOP_K
  - VECTOR_DB_PATH / API keys
- Ensure secrets only via environment variables.

6) API behavior
- Existing endpoints should continue to work.
- Add/extend endpoint to trigger reindexing (admin-safe).
- Response should include retrieval debug info (optional flag):
  - retrieved_chunks
  - similarity_scores
  - selected_rubric_ids

7) Testing
- Add unit/integration tests for:
  - indexing
  - semantic retrieval relevance
  - fallback behavior when vector DB unavailable
  - end-to-end feedback generation with retrieved contexts.

8) Deliverables format
- Give:
  A) Step-by-step implementation plan
  B) Exact file-by-file patch suggestions
  C) Final code snippets
  D) Migration/rollout steps
  E) Local run commands
  F) Validation checklist

Constraints:
- Do not break existing API contracts unless absolutely required.
- Keep implementation modular and easy to swap vector backend later.
- Follow Python best practices and Django conventions.
- Add concise docstrings and comments for non-obvious logic.

Start by:
- Auditing current project structure assumptions.
- Then propose final architecture.
- Then provide code changes in an ordered, executable sequence.

Update my existing Django + React project to use Groq model `llama-3.3-70b-versatile` everywhere relevant.

Important constraints:
- Do not overengineer.
- Keep changes minimal, clean, and consistent with current code style.
- Do not refactor unrelated code.
- Only make targeted edits needed for model migration and docs alignment.

What to change:
1) Backend model defaults
- Replace old/deprecated default model values (e.g. `llama3-70b-8192`) with:
  `llama-3.3-70b-versatile`
- Update all relevant API view defaults and fallback defaults.
- Update any hardcoded model lists shown in API responses.

2) Frontend integration
- Ensure frontend sends/uses `llama-3.3-70b-versatile` as default where model is selected or passed.
- If there is no model selector, keep UI simple and only update internal default payload value.

3) Docs and markdown files
- Update all markdown/docs that mention old model defaults to the new model.
- Keep wording concise; no large documentation rewrite.
- Specifically check project summaries, setup docs, and implementation notes.

4) Tests/scripts/examples
- Update sample requests, test scripts, and examples that reference old model IDs.
- Keep test intent unchanged.

5) Safety checks
- Do not change business logic beyond model-name migration.
- Do not modify database schema.
- Do not introduce new dependencies.

Deliverables:
- Provide a file-by-file change summary.
- Show exact old -> new model string replacements.
- Mention any places intentionally not changed and why.
- Confirm project still runs with the same commands as before.

My goal:
A straightforward migration from deprecated Groq model IDs to `llama-3.3-70b-versatile` with minimal risk and no unnecessary architecture changes.

Migrate my existing Django project from custom orchestration to LangChain + LangGraph with minimal, controlled changes.

Strict constraints:
- Do NOT overengineer.
- Preserve existing underlying behavior, API contracts, and response structure.
- Keep current project structure and file organization as much as possible.
- Do not introduce unnecessary abstractions, extra services, or large redesigns.
- No unrelated refactors.

Primary goal:
Replace current custom RAG/agent orchestration internals with LangChain + LangGraph while keeping external behavior the same.

Required outcomes:

1) Preserve current behavior
- Existing endpoints must keep working with same request/response shapes.
- Keep rubric-based scoring logic semantics the same.
- Keep fallback/error behavior equivalent where possible.
- Maintain current frontend compatibility (no breaking API changes).

2) LangChain migration (RAG internals)
- Move retrieval + prompt assembly + generation flow to LangChain components.
- Keep current retrieval sources (DB rubric data, and vector path if currently supported).
- Keep model/provider wiring through existing environment variables and defaults.
- Do not hardcode secrets.

3) LangGraph migration (agent orchestration)
- Replace current manual single-agent orchestration with a simple LangGraph state graph.
- Graph should mirror existing steps only:
  preprocess -> retrieve rubric/context -> generate feedback -> aggregate/format output
- No multi-agent complexity unless already present.
- Keep state schema minimal and explicit.

4) Keep codebase stable
- Reuse current modules and naming where practical.
- Add only minimal new files needed for LangChain/LangGraph integration.
- Avoid moving many files unless necessary.
- No database schema changes unless absolutely required (prefer none).

5) Testing and validation
- Update existing tests to pass with new internals.
- Add minimal focused tests for new LangChain/LangGraph paths.
- Verify old endpoint-level behavior remains consistent.
- Include a short regression checklist.

6) Documentation updates (important)
- Update all relevant markdown files to reflect migration:
  - architecture docs
  - RAG docs
  - agent docs
  - setup/run docs
  - summary/status docs where model/flow references changed
- Keep docs concise and accurate; no bloated rewrites.

7) Deliverables format
Provide:
A) Migration plan (short, practical)
B) File-by-file change list
C) Code patches/updated code
D) Behavior parity notes (what remained exactly same)
E) Any unavoidable differences (if any) with reasons
F) Updated run/test commands
G) Updated markdown file list

Non-goals:
- No UI redesign
- No new product features
- No advanced agent patterns beyond current workflow needs
- No speculative architecture

Quality bar:
- Clean, readable, minimal diff
- Behavior-first compatibility
- Easy to review and rollback if needed

Implement minimal plagiarism-pattern detection in my existing Django essay feedback system.

Context:
Current system already does:
- file upload/extraction
- paragraph preprocessing
- rubric-based feedback (RAG)
- overall scoring and suggestions

New requirement:
Add “potential plagiarism patterns” detection at a minimal, practical level.
This is NOT full internet plagiarism detection.
It should be local heuristic pattern-flagging only.

Strict constraints:
- Do NOT overengineer.
- Preserve existing API behavior and response structure as much as possible.
- Keep architecture simple and maintainable.
- No heavy external services (no Turnitin-style integration).
- No large refactor.
- No unrelated changes.

What to build (minimal scope):
1) Add a lightweight plagiarism-pattern analyzer module
- New module (e.g., `feedback_system/plagiarism_checker.py`)
- Input: full text + paragraph list
- Output: structured flags with confidence level and reasons

2) Heuristics to implement (simple + deterministic)
- Repetition patterns:
  - unusually repeated long phrases across paragraphs
  - repeated sentence templates
- Citation/style anomalies:
  - sudden citation style shift (e.g., MLA-like to APA-like within same short essay)
- Writing-style inconsistency:
  - abrupt shifts in sentence length/complexity between adjacent paragraphs
- Suspicious patchwork pattern:
  - many paragraphs with high lexical overlap but weak transitions
- Optional local corpus check:
  - if prior submissions exist in DB, compare n-gram overlap against stored texts and flag high overlap
  - keep threshold conservative and configurable

3) Scoring/flag output
- Return:
  - `plagiarism_risk_level`: low | medium | high
  - `plagiarism_flags`: array of objects:
    - `type`
    - `paragraph_indices`
    - `evidence`
    - `confidence` (0-1)
  - `note`: “heuristic pattern detection only, not definitive plagiarism proof”

4) Integrate into existing pipeline
- Hook checker after preprocessing and before final response assembly
- Add plagiarism section into final JSON response without breaking existing fields
- Keep old fields intact

5) Rubric/feedback alignment
- Ensure feedback remains constructive and encouraging
- If plagiarism risk is medium/high, add neutral academic-integrity suggestion in `suggestions`
- Avoid accusatory language

6) Configuration
- Add minimal settings/env toggles:
  - `ENABLE_PLAGIARISM_CHECK=true/false`
  - thresholds (optional, sane defaults)
- No new infra dependencies unless absolutely necessary

7) Tests (minimal but real)
- Unit tests for plagiarism checker heuristics
- Integration test ensuring pipeline response includes plagiarism section
- Regression: existing endpoints still return prior expected keys

8) Docs update
- Update relevant markdown files only:
  - pipeline docs
  - project summary/status docs
  - API response docs
- Clearly state this is heuristic pattern detection, not full external plagiarism verification

Deliverables:
A) Short implementation plan
B) File-by-file changes
C) Code
D) Updated response example
E) Test commands and results summary
F) Notes on limitations and safe usage

Important:
Keep it minimal, transparent, and production-sane.
No overengineering, no complex ML subsystem, no unnecessary abstractions.
